import './filterMenu.css';

const FilterMenu = ({ filterMenuClass }) => {

    return (
        <aside className={filterMenuClass}>
            hello
        </aside>
    );
};

export default FilterMenu;